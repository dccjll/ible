ext {
    //Android config
    config = [
            applicationId: 'com.dcc.ble',
            compileSdkVersion: 27,
            buildToolsVersion: '28.0.3',
            minSdkVersion    : 18,
            targetSdkVersion : 27
    ]

    //Android dependencies
    dependencies = [
            constraintLayoutVersion : '1.1.2',
            nordicsemiDFUVersion : '1.7.0',
            ibaseVersion: '1.0.2'
    ]

    // APP config 全局配置
    app = [
            versionCode: 2,
            versionName: '1.0.1'
    ]
}